import 'package:flutter/material.dart';

class FloorInput extends StatelessWidget {
  const FloorInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
